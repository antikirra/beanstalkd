# Build output — the same set `make clean` removes. The generated test
# harness is covered by ct/.gitignore, which is vendored with it.
*.o
*.gcda
*.gcno
*.gcov
/beanstalkd
/vers.c
.cflags-stamp

# bench/ leavings: run output, and the caches Python drops beside the
# scripts one bench imports from another.
bench/results/
__pycache__/
